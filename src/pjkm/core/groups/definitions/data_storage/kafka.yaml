id: kafka
name: "Apache Kafka"
description: "Kafka event streaming with confluent-kafka and async support"
category: "Data & Storage"
archetypes:
  - service
  - poly_repo
requires_groups: []
platform_filter: null

dependencies:
  kafka:
    - "confluent-kafka>=2.8.0"
    - "aiokafka>=0.12.0"
    - "faust-streaming>=0.11.0"

scaffolded_files:
  - template_fragment: "compose_kafka"
    destination: "."
    description: "Docker Compose service definition for Kafka"

pyproject_tool_config: {}
