"""SQLAlchemy models for {{ project_name }}.

Import all models here so Alembic can discover them.
"""

from {{ project_slug }}.core.database import Base  # noqa: F401

# Import your models below so they're registered with Base.metadata:
# from {{ project_slug }}.models.user import User  # noqa: F401
