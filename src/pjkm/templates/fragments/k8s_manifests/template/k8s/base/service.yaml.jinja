apiVersion: v1
kind: Service
metadata:
  name: {{ project_slug }}
  labels:
    app: {{ project_slug }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: {{ project_slug }}
