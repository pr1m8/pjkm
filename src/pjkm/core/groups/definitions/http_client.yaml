id: http_client
name: "HTTP Client"
description: "httpx async client, respx for testing, retry/backoff, rate limiting"
archetypes: []
requires_groups: []
platform_filter: null

dependencies:
  http_client:
    - "httpx>=0.28.0"
    - "respx>=0.22.0"
    - "stamina>=24.3.0"
    - "hishel>=0.1.0"

scaffolded_files: []

pyproject_tool_config: {}
