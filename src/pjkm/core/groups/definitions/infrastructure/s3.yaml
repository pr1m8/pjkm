id: s3
name: "S3 / Object Storage"
description: "S3-compatible object storage with MinIO for local dev"
category: "Infrastructure"
archetypes:
  - service
  - poly_repo
requires_groups: []
platform_filter: null

dependencies:
  s3:
    - "boto3>=1.35.0"
    - "aioboto3>=13.0.0"
    - "minio>=7.2.0"

scaffolded_files: []

pyproject_tool_config: {}
