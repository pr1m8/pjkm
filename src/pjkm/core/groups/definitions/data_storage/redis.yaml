id: redis
name: "Redis"
description: "Redis async client for caching, sessions, and rate limit backends"
category: "Data & Storage"
archetypes:
  - service
  - poly_repo
requires_groups: []
platform_filter: null

dependencies:
  redis:
    - "redis[hiredis]>=5.2.0"

scaffolded_files:
  - template_fragment: "compose_redis"
    destination: "."
    description: "Docker Compose service definition for Redis"
  - template_fragment: "redis_client"
    destination: "."
    description: "Redis async client module with connection pool and health check"

pyproject_tool_config: {}
