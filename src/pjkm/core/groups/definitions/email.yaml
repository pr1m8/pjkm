id: email
name: "Email"
description: "Email sending via SMTP and templating with Jinja2"
archetypes:
  - service
requires_groups: []
platform_filter: null

dependencies:
  email:
    - "aiosmtplib>=3.0.0"
    - "email-validator>=2.2.0"
    - "jinja2>=3.1.0"
    - "premailer>=3.10.0"

scaffolded_files: []

pyproject_tool_config: {}
