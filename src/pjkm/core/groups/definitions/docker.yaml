id: docker
name: "Docker & Containers"
description: "Dockerfile, .dockerignore, devcontainer, and multi-stage build for Python 3.13 with PDM"
archetypes:
  - service
  - poly_repo
requires_groups: []
platform_filter: null

dependencies: {}

scaffolded_files:
  - template_fragment: "docker_python"
    destination: "."
    description: "Dockerfile (multi-stage), .dockerignore, devcontainer.json, compose.dev.yml"
  - template_fragment: "github_actions_docker"
    destination: "."
    description: "Docker security scan workflow (Hadolint + Trivy)"

pyproject_tool_config: {}
