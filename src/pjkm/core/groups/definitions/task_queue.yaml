id: task_queue
name: "Task Queue (Dramatiq)"
description: "Dramatiq task queue with Redis broker and results backend"
archetypes:
  - service
  - poly_repo
requires_groups: []
platform_filter: null

dependencies:
  task_queue:
    - "dramatiq[redis,watch]>=1.17.0"
    - "apscheduler>=3.11.0"
    - "periodiq>=0.12.1"

scaffolded_files: []

pyproject_tool_config: {}
