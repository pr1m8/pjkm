id: frontend_vite
name: "Frontend (Vite + React)"
description: "Vite + React + TypeScript SPA with API proxy to Python backend"
category: "Frontend"
archetypes:
  - service
  - poly_repo
requires_groups: []
platform_filter: null

dependencies: {}

scaffolded_files:
  - template_fragment: "frontend_vite"
    destination: "."
    description: "Vite + React + TypeScript SPA with dev server proxy"

pyproject_tool_config: {}
