id: rabbitmq
name: "RabbitMQ"
description: "RabbitMQ message broker with pika, aio-pika, and kombu"
category: "Data & Storage"
archetypes:
  - service
  - poly_repo
requires_groups: []
platform_filter: null

dependencies:
  rabbitmq:
    - "pika>=1.3.0"
    - "aio-pika>=9.5.0"
    - "kombu>=5.5.0"

scaffolded_files:
  - template_fragment: "compose_rabbitmq"
    destination: "."
    description: "Docker Compose service definition for RabbitMQ"

pyproject_tool_config: {}
