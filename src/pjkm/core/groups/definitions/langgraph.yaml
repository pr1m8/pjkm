id: langgraph
name: "LangGraph Runtime"
description: "LangGraph agent orchestration with checkpoint persistence and prebuilt patterns"
archetypes: []
requires_groups: [langchain]
platform_filter: null

dependencies:
  langgraph:
    - "langgraph>=1.0.0"
    - "langgraph-sdk>=0.3.0"
    - "langgraph-prebuilt>=1.0.0"
    - "langgraph-checkpoint-postgres>=2.0.0"
    - "langmem>=0.0.30"
    - "langsmith>=0.7.0"

scaffolded_files: []
pyproject_tool_config: {}
