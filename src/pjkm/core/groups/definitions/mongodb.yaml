id: mongodb
name: "MongoDB"
description: "MongoDB with Motor async driver, Beanie ODM, and pymongo"
archetypes:
  - service
  - poly_repo
requires_groups: []
platform_filter: null

dependencies:
  mongodb:
    - "motor>=3.7.0"
    - "beanie>=1.29.0"
    - "pymongo>=4.12.0"

scaffolded_files: []

pyproject_tool_config: {}
