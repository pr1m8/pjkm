id: file_upload
name: "File Upload & Storage"
description: "S3-compatible file uploads with presigned URLs, thumbnails, and streaming"
category: "Web & API"
archetypes:
  - service
requires_groups:
  - api
platform_filter: null

dependencies:
  file_upload:
    - "boto3>=1.35.0"
    - "aioboto3>=13.0.0"
    - "python-multipart>=0.0.20"
    - "filetype>=1.2.0"

scaffolded_files: []

pyproject_tool_config: {}
